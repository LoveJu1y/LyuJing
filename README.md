# LyuJing personal homepage

A first visual version of LyuJing's personal research homepage.

## Run locally

This version is a dependency-free static site. From this folder:

```powershell
python -m http.server 4173
```

Then open `http://localhost:4173`.

The main files are `index.html`, `styles.css`, and `script.js`.
